app = 'tsidp-old-water-2360'
primary_region = 'fra'

[build]
# renovate: datasource=docker depName=ghcr.io/pocket-id/pocket-id versioning=semver
  image = 'ghcr.io/pocket-id/pocket-id:v1.16.0-distroless'

[deploy]
  strategy = 'rolling'

[env]
  APP_URL = 'https://auth.technat.dev'
  TRUST_PROXY = 'true'
  FILE_BACKEND = 's3'
  S3_BUCKET = 'pocket-id'
  S3_ENDPOINT = 'https://t3.storage.dev'
  S3_REGION = 'auto'
  KEYS_STORAGE = 'database'
  SMTP_FROM = 'systems@technat.dev'
  SMTP_TLS = 'starttls'

[[mounts]]
  source = 'data'
  destination = '/app/data'
  initial_size = '1GB'
  snapshot_retention = 7
  scheduled_snapshots = true
  auto_extend_size_threshold = 90
  auto_extend_size_increment = '1GB'
  auto_extend_size_limit = '5GB'

[http_service]
  internal_port = 1411
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '2s'
    method = 'GET'
    path = '/healthz'

[[vm]]
  memory = '256mb'
  cpus = 1
